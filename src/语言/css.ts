import { 语言T } from '.';

export const css: 语言T = {
    补全锚点配置: {
        是否标识符字符: (字符) => /[a-zA-Z0-9_-]/.test(字符), // CSS 属性/值常含 -
        语法边界字符: new Set([
            ':',    // 属性后（color: ）
            ' ',    // 选择器后或值列表（margin: 10px ）
            '(',    // 函数值（rgb(）
            ',',    // 函数参数分隔（rgba(255, 0, 0, )
            '{',    // 规则块开始（.cls {）
        ]),
        最大回退距离: 30,
    },
    触发字符: [
        ':', ' ', '(', ',', '{',
    ],
};