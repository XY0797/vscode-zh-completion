import { 语言T } from '.';

export const json: 语言T = {
    补全锚点配置: {
        是否标识符字符: (字符) => /[a-zA-Z0-9_]/.test(字符), // JSON key 通常为简单标识符
        语法边界字符: new Set([
            ':',    // 键后（"key": ）
            ',',    // 元素分隔（{ "a": 1, ）
            '{',    // 对象开始（{ "key": ）
            '[',    // 数组开始（[ 1, ）
        ]),
        最大回退距离: 15,
    },
    触发字符: [
        ':', ',', '{', '[',
    ],
};
